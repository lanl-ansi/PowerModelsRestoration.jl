

function rad_heuristic(data)

    # initial ordering (utilization heuristic)
    repair_ordering = utilization_heuristic_restoration(data)

    while !false
        #partition sequence
        #solve decoupled ROPs on each sequence
    end
    return repair_ordering
end
